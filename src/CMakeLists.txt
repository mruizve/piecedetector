#deriche method for edge detection
#subdirs( deriche )

# pieces/shapes detectors
subdirs( detector )

# flexibowl library
subdirs( flexibowl )

# image grabber (from blaster camera)
subdirs( grabber )

# network library
subdirs( network )

# utils library
subdirs( utils )
