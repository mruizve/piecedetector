# Dediche method for edge detection
#subdirs( deriche )

# filters library
subdirs( operators )

# pieces/shapes detector 
subdirs( detector )

# image grabber (from blaster camera)
subdirs( grabber )
